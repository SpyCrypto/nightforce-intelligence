[build]
  command = "pnpm install && cd counter-contract && pnpm build && cd ../frontend-vite-react && pnpm build:netlify"
  publish = "frontend-vite-react/dist"

[build.environment]
  NODE_VERSION = "22"
  PNPM_VERSION = "10.14.0"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
